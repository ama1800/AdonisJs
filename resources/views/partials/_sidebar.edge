
<div class="my-2">
    <a href="{{ route('newTag') }}" class="btn btn-primary">New post</a>
</div>

<div class="mt-3">
    <h4 class="my-2">Tags</h4>
    <div class="list-group">
        @each(tag in tags)
        <a href="#" class="list-group-item list-group-item-action">{{ tag.name }}</a>          
        @else
          <span>Vide..!</span>
        @endeach
      </div>
</div>